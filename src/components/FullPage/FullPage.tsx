import { Layout } from "../Layout";
import { FullPageWrapper } from "./FullPageComponents/FullPageWrapper";

export const FullPage = () => {
    return (
        <Layout>
            <FullPageWrapper />
        </Layout>
    );
};
